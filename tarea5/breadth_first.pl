adjacent(zerind, arad).
adjacent(arad, zerind).

adjacent(oradea, zerind).
adjacent(zerind, oradea).

adjacent(oradea, sibiu).
adjacent(sibiu, oradea).

adjacent(arad, sibiu).
adjacent(sibiu, arad).

adjacent(arad, timisoara).
adjacent(timisoara, arad).

adjacent(timisoara, lugoj).
adjacent(lugoj, timisoara).

adjacent(lugoj, mehadia).
adjacent(mehadia, lugoj).

adjacent(mehadia, drobeta).
adjacent(drobeta, mehadia).

adjacent(drobeta, craiova).
adjacent(craiova, drobeta).

adjacent(sibiu, fagaras).
adjacent(fagaras, sibiu).

adjacent(sibiu, rimnicu_vilcea).
adjacent(rimnicu_vilcea, sibiu).

adjacent(rimnicu_vilcea, pitesti).
adjacent(pitesti, rimnicu_vilcea).

adjacent(rimnicu_vilcea, craiova).
adjacent(craiova, rimnicu_vilcea).

adjacent(pitesti, bucharest).
adjacent(bucharest, pitesti).

adjacent(giurgiu, bucharest).
adjacent(bucharest, giurgiu).

adjacent(bucharest, urziceni).
adjacent(urziceni, bucharest).

adjacent(urziceni, hirsova).
adjacent(hirsova, urziceni).

adjacent(hirsova, eforie).
adjacent(eforie, hirsova).

adjacent(urziceni, vaslui).
adjacent(vaslui, urziceni).

adjacent(vaslui, iasi).
adjacent(iasi, vaslui).

adjacent(iasi, neamt).
adjacent(neamt, iasi).

% Added city
adjacent(ganeasa, rimnicu_vilcea).
adjacent(rimnicu_vilcea, ganeasa).

adjacent(ganeasa, craiova).
adjacent(craiova, ganeasa).

